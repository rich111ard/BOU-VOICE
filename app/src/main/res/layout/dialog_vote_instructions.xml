<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Title -->
    <TextView
        android:id="@+id/vote_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voting Instructions"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="8dp" />

    <!-- Instruction content -->
    <TextView
        android:id="@+id/vote_dialog_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This section allows you to cast your vote for different nominees. You can view the candidates and vote for your preferred option."
        android:textSize="16sp" />

    <!-- Button layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:paddingTop="16dp">

        <!-- Close Button -->
        <Button
            android:id="@+id/vote_close_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Close" />

        <!-- Proceed Button -->
        <Button
            android:id="@+id/vote_proceed_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Proceed"
            android:layout_marginStart="16dp" />
    </LinearLayout>

</LinearLayout>
